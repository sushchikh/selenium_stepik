# browser.switch_to.window(window_name)  # переключит на другую вкладку, если она есть
# current_window = browser.current_window_handle  # покажет текущую вкладку
# new_window = browser.window_handles[1]  # из массива вкладок выберет вторую, дальше browser.switch_to.window(new_window)
# first_window = browser.window_handles[0]  # запомним старую вкладку, чтобы к ней вернуться

